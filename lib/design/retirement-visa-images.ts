import { livingImages } from "@/lib/design/living-images";

/** Retirement Visa page — verified image URLs from Living Stitch assets */
export const retirementVisaImages = {
  hero: livingImages.retirementVisa,
} as const;
